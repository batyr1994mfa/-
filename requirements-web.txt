flask>=3.0.0
requests>=2.31.0
psutil>=5.9.0
# Необязательные зависимости для расширенных инструментов ИИ.
# Если какой-то из них не установлен, соответствующий инструмент вернёт
# понятную ошибку с подсказкой, что установить — сервер не упадёт.
pdfplumber>=0.10.0
python-docx>=1.1.0
openpyxl>=3.1.0
pytesseract>=0.3.10
Pillow>=10.0.0
