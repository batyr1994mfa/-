Claude, посмотри на нашу правую панель «Активность в файлах». Сейчас там отображается статичный текстовый список с иконками, который просто перечисляет твои навыки (создание, удаление, редактирование файлов, команды терминала, Википедия / Google / GitHub, память, уведомления, поиск фото и архив решений).

Давай превратим эту панель в современный, технологичный и компактный пульт управления ИИ-агентом! 

Выполни следующие задачи на фронтенде (index.html) и бэкенде (server.py) и пересобери проект в новый архив:

1. ЖИВЫЕ ТУМБЛЕРЫ УПРАВЛЕНИЯ (ВМЕСТО КНОПКИ «ПРОМТ»):
Преврати этот правый список в интерактивную панель переключателей. Добавь напротив каждой основной группы инструментов аккуратный CSS-тумблер (toggle) или чекбокс.
Сгруппируй твои ~35 внутренних инструментов под тумблеры на панели строго по пунктам с экрана:
- «Работа с файлами» (объединяет создание, удаление, редактирование, чтение)
- «Команды терминала» (управляет запуском консоли)
- «Поиск в Сети» (Википедия / Google / GitHub / Поиск фото и видео)
- «Память и Проекты» (память, процессы, git, службы, документы, OCR, архивы)
- «Архив успешных решений» (управление твоим RAG-модулем)
- «Логи и Уведомления» (telegram, уведомления) — *напоминаю, модули Почты (send_email) и Push (notify) полностью вырежи из кода и этой панели, как мы договаривались!*

2. ДИНАМИЧЕСКИЙ PAYLOAD НА БЭКЕНДЕ:
Массив `tools` в API-запросе к Ollama должен собираться строго на основе включенных пользователем тумблеров на правой панели. Если тумблер (например, «Архив решений» или «Команды терминала») выключен — описания этих инструментов физически не отправляются в системный промпт. Это спасет видеокарту от перегрева.
- Если все тумблеры выключены, а сообщение пользователя короткое (до 20 символов, например "Ты тут?"), должен автоматически срабатывать чистый "Турбо-режим" без единого инструмента и с параметром "think": false. Ответ должен прилетать за 1 секунду.

3. ТЕХНОЛОГИЧНЫЙ ВИЗУАЛ И КОМПАКТНОСТЬ (Стиль Dashboard):
- Сделай панель компактнее: уменьши шрифты и отступы, чтобы весь список аккуратно занимал верхнюю часть экрана без прокрутки.
- Добавь микро-анимации статуса: когда инструмент включен и ИИ прямо сейчас его использует (например, читает файл или ищет в сети), соответствующий пункт на панели должен плавно пульсировать синим неоновым цветом (animation: pulse).
- После успешного выполнения операции пункт должен на секунду подсвечиваться зеленым цветом с мини-галочкой (✔), а при ошибке — красным крестиком (✖). Если инструмент отдыхает — иконка горит стандартным приглушенным цветом.
- Технические детали (коды возврата, логи выполнения терминала) по умолчанию скрывай под спойлер. Они должны плавно раскрываться вниз (аккордеоном) только при клике на конкретную строку активности.


Claude, у нас гениальная идея по радикальной оптимизации! Мы сделаем интеллектуальный предварительный анализ запроса (Speculative Pre-processing) прямо в момент нажатия кнопки отправки, чтобы Ollama никогда не загружалась вхолостую.

Пожалуйста, добавь в роут `/api/chat` в `server.py` и в JS-код фронтенда следующую логику:

1. ЭТАП ПРЕДВАРИТЕЛЬНОГО АНАЛИЗА (Пауза оптимизации):
Когда я нажимаю кнопку «Отправить», запрос НЕ должен сразу улетать в тяжелую модель Ollama. Сначала бэкенд на чистом Python должен мгновенно проанализировать текст сообщения (`user_text`) на наличие ключевых маркеров и потребностей в инструментах. На фронтенде в этот момент в зоне активности должно красиво отображаться: «🧠 Анализ запроса и подбор навыков...».

2. АВТО-ПЕРЕКЛЮЧЕНИЕ ТУМБЛЕРОВ НА ПРАВОЙ ПАНЕЛИ:
Напиши легкую функцию-классификатор, которая по ключевым словам сама определяет, какие тумблеры на нашей правой панели нужно активировать для этого конкретного сообщения:
- Если в тексте есть: "файл", "папка", "загрузки", "прочитай", "создай" -> сервер автоматически включает тумблер «Работа с файлами».
- Если в тексте есть: "ярлык", "терминал", "запусти", "bash", "команда", "установи" -> автоматически включается тумблер «Команды терминала».
- Если в тексте есть: "найди в интернете", "погода", "гугл", "яндекс", "курс", "ссылка" -> включается тумблер «Поиск в Сети».
- Если это короткий чек ("Ты тут?", "Привет", "Да") -> ВСЕ тумблеры автоматически выключаются, активируя чистый "Турбо-режим".

3. СБОРКА ИДЕАЛЬНОГО ПРОМПТА:
После того как авто-фильтр выбрал нужные тумблеры (например, только Файлы + Терминал для создания ярлыка), бэкенд подшивает в get_tools_schema() описания ТОЛЬКО ЭТИХ двух инструментов. Все остальные 30+ инструментов полностью блокируются. В Ollama уходит идеально чистый, легкий и точечный запрос.

4. ВИЗУАЛИЗАЦИЯ ДЛЯ ПОЛЬЗОВАТЕЛЯ:
Сделай так, чтобы во время этой секундной паузы анализа я видел на правой панели, как тумблеры сами перещелкиваются в нужные положения под мой запрос, подсвечивая активные навыки ИИ.

Интегрируй этот интеллектуальный пре-процессинг во все связанные модули и обнови наш итоговый ZIP-архив!     ----- вот что я ему хочу отправить нужно немного ПРОМТ исправить, он мне делает кнопку (ПРОМТ) нажимая на нее внутри будет возможность управлять вручную и если тумблеры все включены то он грузит тяжелый промт, рядом же сделать режим (АВТО).
